# Build output. Regenerated by tools/build.py; never edited, never committed.
out/

# Agent tooling. Kept in the project folder on purpose (the work moves between
# machines), ignored because it holds local settings and working notes.
.claude/

# Local tooling only. package.json is committed so the toolchain is
# reproducible; the installed tree is not.
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
